import React from "react";
import "../sass/Tip.scss";

const Tip = () => {
  return (
    <div id="tip">
      <span style={{ visibility: "hidden" }}>Tip</span>
    </div>
  );
};

export default Tip;
